FROM node:22-alpine

# Build tools kept in case native deps (bcrypt, pg-native, etc.) get added later
RUN apk add --no-cache python3 make g++

WORKDIR /app

COPY package.json yarn.lock ./
RUN yarn install --frozen-lockfile

COPY . .

EXPOSE 3000

CMD ["yarn", "start:dev"]
